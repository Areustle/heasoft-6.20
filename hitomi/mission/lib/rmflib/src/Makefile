HD_COMPONENT_NAME	= hitomi

HD_COMPONENT_VERS	= 

HD_LIBRARY_ROOT		= rmflib

HD_LIBRARY_SRC_cxx	= rmflib.cxx

HD_CXXFLAGS		= ${HD_STD_CXXFLAGS}

HD_SHLIB_LIBS		= ${HD_LFLAGS} -l${AHFITS} -l${HEASP} -l${CCFITS} ${AH_STD_LIBS} -l${HEAUTILS}

HD_INSTALL_LIBRARIES	= ${HD_LIBRARY_ROOT}

include ${HD_STD_MAKEFILE}
