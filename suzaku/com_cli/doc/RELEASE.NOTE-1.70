com_cli version 1.61 -> 1.70 の変更点

  o CLI 変数の再利用に関するバグの修正 (これまで再利用ができなくなっていた)

  o MODVAL, INQUIRE で、"#N" で N 番めのパラメータを参照できるようにした

  o LUNRD/OPNRD 中において "@filename" でファイルを読み込んだ時に、
    スタックがおかしくなるバグの修正

  o KEYRD において、ABC と ABCD のように名前が重なるものがあった場合、
    ABC と入力することで ABC を選ぶことができるように仕様変更

  o CLVERS, HEXRDX の関数を追加

  o CLI の特殊コマンドに以下のものを追加 (cli-userguide.doc より)

@.version

  Show CLI version string.

