HD_COMPONENT_NAME	= swift

HD_COMPONENT_VERS	=

HD_CTASK		= batid2xy

HD_CTASK_SRC_c		= batid2xy.c 

HD_CFLAGS		= ${HD_STD_CFLAGS}

HD_CLIBS		= ${HD_LFLAGS} -lbatutils ${HD_STD_LIBS} \
			  ${SYSLIBS}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= batid2xy.par

HD_INSTALL_HELP		= batid2xy.html

HD_TEST_PERL_SCRIPTS    = ut_batid2xy.pl

include ${HD_STD_MAKEFILE}
