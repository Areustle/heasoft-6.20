HD_COMPONENT_NAME	= swift

HD_COMPONENT_VERS	=

HD_CTASK		= uvotimsum1

HD_CTASK_SRC_c		= uvotimsum1.c

HD_CFLAGS		= ${HD_STD_CFLAGS}

LOC_LIBS		= -lcoordfits -lcoord -lrew

HD_CLIBS		= ${HD_LFLAGS} ${LOC_LIBS} ${HD_STD_LIBS} ${SYSLIBS} ${F77LIBS4C}

HD_INSTALL_PERL_SCRIPTS	= uvotimsum

HD_TEST_PERL_SCRIPTS	= ut-uvotimsum

HD_TEST_PFILES		= ut-uvotimsum.par

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= uvotimsum.par uvotimsum1.par

HD_INSTALL_HELP		= uvotimsum.html

include ${HD_STD_MAKEFILE}
