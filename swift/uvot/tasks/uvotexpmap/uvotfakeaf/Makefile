HD_COMPONENT_NAME	= swift

HD_COMPONENT_VERS	=

HD_CTASK		= uvotfakeaf

HD_CTASK_SRC_c		= uvotfakeaf.c

HD_CFLAGS		= ${HD_STD_CFLAGS}

LOC_LIBS		= -g -lcoordfits -lcoord -lswuvot -lrew

HD_CLIBS		= ${HD_LFLAGS} ${LOC_LIBS} ${HD_STD_LIBS} ${SYSLIBS} ${F77LIBS4C}

HD_INSTALL_PERL_SCRIPTS	= 

HD_TEST_PERL_SCRIPTS	= ut-uvotfakeaf

HD_TEST_PFILES		= ut-uvotfakeaf.par

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= uvotfakeaf.par

HD_INSTALL_HELP		= uvotfakeaf.html

include ${HD_STD_MAKEFILE}
