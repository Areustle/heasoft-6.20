HD_COMPONENT_NAME	= swift

HD_COMPONENT_VERS	= 

HD_CTASK		= rectext

HD_CTASK_SRC_c		= extract.c rectext.c

HD_CFLAGS		= ${HD_STD_CFLAGS} -I./include

HD_CLIBS		= ${HD_LFLAGS} -lrew ${HD_STD_LIBS} ${SYSLIBS}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PERL_SCRIPTS	= uvotimgrism

HD_TEST_PERL_SCRIPTS	= ut-uvotimgrism

HD_INSTALL_PFILES	= rectext.par uvotimgrism.par

HD_TEST_PFILES		= ut-uvotimgrism.par

HD_INSTALL_HELP		= uvotimgrism.html

include ${HD_STD_MAKEFILE}
