HD_COMPONENT_NAME	= ftools

HD_COMPONENT_VERS	= 

HD_CTASK		= spibkg_init

HD_CTASK_SRC_c		= hspibkg_init.c spibkg_init.c

HD_CFLAGS		= ${HD_STD_CFLAGS}

HD_CLIBS		= ${HD_STD_CLIBS} ${F77LIBS4C}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= spibkg_init.par

HD_INSTALL_HELP		= spibkg_init.txt

include ${HD_STD_MAKEFILE}
