HD_COMPONENT_NAME	= ftools

HD_COMPONENT_VERS	= 

HD_CTASK		= fovdsp

HD_CTASK_SRC_c		= hfovdsp.c

HD_CTASK_SRC_f		= fovdsp.f

HD_CFLAGS		= ${HD_STD_CFLAGS}

HD_FFLAGS		= ${HD_STD_FFLAGS}

HD_CLIBS		= ${HD_LFLAGS} ${LINK_PGPLOT} \
			  ${HD_STD_LIBS} ${XLIBS} ${SYSLIBS} ${F77LIBS4C}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= fovdsp.parin

HD_INSTALL_HELP		= fovdsp.txt

include ${HD_STD_MAKEFILE}
