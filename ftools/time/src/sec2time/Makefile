HD_COMPONENT_NAME	= ftools

HD_COMPONENT_VERS	= 

HD_CTASK		= sec2time

HD_CTASK_SRC_c		= hsec2time.c

HD_CTASK_SRC_f		= sec2time.f

HD_CFLAGS		= ${HD_STD_CFLAGS}

HD_FFLAGS		= ${HD_STD_FFLAGS}

HD_CLIBS		= ${HD_STD_CLIBS} ${F77LIBS4C}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= sec2time.parin

HD_INSTALL_HELP		= sec2time.hlp sec2time.txt

include ${HD_STD_MAKEFILE}
