HD_COMPONENT_NAME	= ftools

HD_COMPONENT_VERS	= 

HD_CTASK		= detect

HD_CTASK_SRC_c		= hdetect.c

HD_CTASK_SRC_f		= detect.f

HD_CFLAGS		= ${HD_STD_CFLAGS}

HD_FFLAGS		= ${HD_STD_FFLAGS}

HD_CLIBS		= ${HD_LFLAGS} -lros${FT_VERS} \
			  ${HD_STD_LIBS} ${SYSLIBS} ${F77LIBS4C}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= detect.parin

HD_INSTALL_HELP		= detect.hlp detect.txt

HD_INSTALL_REFDATA	= detect_text.hash

include ${HD_STD_MAKEFILE}
