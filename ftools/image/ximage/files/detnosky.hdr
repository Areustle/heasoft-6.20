#
#  Template for detect output in FITS format
#   (omit RA/Dec references when no sky coordinates)
#
#  ! = integer, $ = string, # = double
#
FILE     = $         / Filename
OBJECT   = $         / Field identification
TELESCOP = $         / Telescope
INSTRUME = $         / Instrument
DETNAM   = $         / Detector name
FILTER   = $         / Filter
EQUINOX  = #         / Equinox
EXPOSURE = #         / Exposure time (sec)
DATE-OBS = $         / Date of observation begin
DATE-END = $         / Date of observation end
CREATOR  = \XIMAGE   / Program that produced this file
\wcs history
