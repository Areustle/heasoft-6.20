HD_COMPONENT_NAME	= ftools

HD_COMPONENT_VERS	= 

HD_CTASK		= hextebackest

HD_CTASK_SRC_c		= hextebackest.c

HD_CFLAGS		= ${HD_STD_CFLAGS} -I./include

HD_CLIBS		= ${HD_LFLAGS} -l${PIL} -l${HEAUTILS} ${HD_STD_LIBS} ${SYSLIBS} ${F77LIBS4C}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PFILES	= hextebackest.par

HD_INSTALL_HELP		= hextebackest.txt

include ${HD_STD_MAKEFILE}
