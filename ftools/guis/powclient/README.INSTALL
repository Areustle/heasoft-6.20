1) To install,
./configure --prefix=your_root_install_directory
make
make install
2) To clean,
make distclean              (Makefile is also deleted)
or 
make clean                  (only delete the .o files)

