c Max Array sizes (Disable the maxchan, maxne, maxgrp)
      integer  MXARFS
      parameter (MXARFS= 2500)
      integer maxhist, maxcomm
      integer maxextn
      parameter (maxhist=20,maxcomm=8*MXARFS, maxextn = 99)
C     integer maxchan, maxne, maxgrp
C     parameter (maxhist=20,maxcomm=800,maxgrp=5)
C     parameter (maxchan=4096,maxne=2048, maxextn=99)


