! Normalized GAUSsian.  Similar to the PLT builtin function GA, except
! that the norm is the integrated area in gaussion.
! P1 = GC
! P2 = GW
! P3 = GN (total area in gaussian)
!
: ngaus
p1 x - p2 / dup * 2 / neg exp p3 p2 / 2.50663 / *
;
