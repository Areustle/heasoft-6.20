# FUTILS - FITS tools package.

    doc/		futils package documentation (help files)
    futils/		futils task sources
    futils.cl		futils package script - loads futils CL package
    futils.hd		futils help directory
    futils.men		futils menu listing
    futils.par		futils package parameter file
    lib/		futils package library
    mkpkg		used to build the futils package: mkpkg -p futils
    testdata/		some FITS test files

    unix/		host interface for host level version of package

    bin			linked to current architecture
    bin.generic		source-only (generic) architecture
    bin.sparc		executables for sparc architecture
    bin.f68881		executables for Sun-3, f68881 floating point
    bin.dsux		executables for decstation
        (etc.)
