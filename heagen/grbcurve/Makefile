HD_COMPONENT_NAME	= heagen

HD_COMPONENT_VERS	=

HD_CTASK		= grbcurve

HD_CTASK_SRC_c		= ${HD_CTASK}.c

HD_CFLAGS		= ${HD_STD_CFLAGS}

HD_CLIBS		= ${HD_LFLAGS} -lxronos ${HD_STD_LIBS} ${SYSLIBS}

HD_INSTALL_TASKS	= ${HD_CTASK}

HD_INSTALL_PERL_SCRIPTS = 

HD_INSTALL_PFILES	= ${HD_CTASK}.par

HD_INSTALL_HELP		= ${HD_CTASK}.html

HD_TEST_PERL_SCRIPTS	= 

HD_TEST_PFILES		= 

include ${HD_STD_MAKEFILE}
